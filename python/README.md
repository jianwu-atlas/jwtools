# jwtools / python

本目录存放与 `jwtools` R 包配套的 Python 可视化工具模块，用于生成
**Up/Down 双面板 DEG presence heatmap**（适用于任意"cell type ×
多组比较"的二值 presence matrix 数据可视化场景）。

## 安装依赖

```bash
pip install -r requirements.txt

