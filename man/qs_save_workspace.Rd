% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qs_workspace.R
\name{qs_save_workspace}
\alias{qs_save_workspace}
\title{保存当前环境所有变量到一个 .qs2 文件}
\usage{
qs_save_workspace(
  file,
  envir = .GlobalEnv,
  nthreads = 6,
  exclude = NULL,
  exclude_pattern = NULL,
  verbose = TRUE
)
}
\arguments{
\item{file}{输出文件路径，建议以 \code{.qs2} 结尾。}

\item{envir}{要保存的环境，默认 \code{.GlobalEnv}（你当前的工作环境）。}

\item{nthreads}{压缩/写入线程数，默认 6。}

\item{exclude}{字符向量，按变量名精确排除（例如已经单独存过的大对象，
避免重复占用磁盘）。默认不排除。}

\item{exclude_pattern}{正则表达式字符串，按变量名模式排除（例如
\code{"^p_|^tmp_"} 排除所有以 \code{p_} 或 \code{tmp_} 开头的变量）。
默认不排除。}

\item{verbose}{是否打印排除信息、变量大小排行和保存结果。默认 \code{TRUE}。}
}
\value{
不可见地返回被保存的变量名字符向量（\code{invisible()}）。
}
\description{
对应你原来用的 \code{save(list = ls(all = TRUE), file = "xxx.RData")}，
但用 \pkg{qs2} 压缩/读写，速度更快、体积更小，尤其适合装着 Seurat 对象
的大型工作环境。
}
\examples{
\dontrun{
# 保存整个工作环境
qs_save_workspace("core_WT_YMO_workspace.qs2", nthreads = 14)

# 排除已经单独存过的大对象，避免重复保存
qs_save_workspace(
  "core_WT_YMO_workspace.qs2",
  nthreads = 14,
  exclude = c("immune.combined")
)

# 按正则排除所有 ggplot / 临时对象
qs_save_workspace(
  "core_WT_YMO_workspace.qs2",
  nthreads = 14,
  exclude_pattern = "^p_|^tmp_|^fig[0-9]"
)
}
}
