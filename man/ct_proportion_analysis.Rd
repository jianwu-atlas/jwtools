% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct_proportion_analysis.R
\name{ct_proportion_analysis}
\alias{ct_proportion_analysis}
\title{细胞类型比例的分组统计分析与可视化（支持dataset/batch overlay）}
\usage{
ct_proportion_analysis(
  seurat_obj,
  celltype_col = "Manuscript_Identity",
  sample_col = "sample",
  group_col = "AgeGroup_short",
  group_levels = NULL,
  group_labels = NULL,
  group_colors = NULL,
  dataset_col = NULL,
  dataset_colors = NULL,
  dataset_shapes = NULL,
  celltype_order = NULL,
  celltype_colors = NULL,
  comparisons = NULL,
  p_adjust_method = "BH",
  facet_nrow = 2,
  output_prefix = "CTprop",
  save_plots = TRUE,
  fig_width = 15,
  fig_height = 7.5,
  label_outliers = FALSE,
  outlier_coef = 1.5,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{Seurat对象，或直接传入包含相应列的 data.frame}

\item{celltype_col}{细胞类型列名，默认 "Manuscript_Identity"}

\item{sample_col}{样本(donor)ID列名，默认 "sample"}

\item{group_col}{分组变量列名（如年龄组），默认 "AgeGroup_short"}

\item{group_levels}{分组变量的顺序，NULL则自动从factor levels/排序推断}

\item{group_labels}{分组变量的图例显示文字（named vector），NULL则用原始值}

\item{group_colors}{分组变量的boxplot填充色（named vector），NULL则自动配色}

\item{dataset_col}{数据集/批次来源列名，NULL则不绘制dataset overlay}

\item{dataset_colors}{dataset的散点颜色（named vector），NULL则自动生成}

\item{dataset_shapes}{dataset的散点形状（named vector），NULL则自动生成}

\item{celltype_order}{celltype在facet中的排列顺序，NULL则自动推断}

\item{celltype_colors}{celltype对应的strip文字颜色（named vector），NULL则灰色}

\item{comparisons}{两两比较列表，如 list(c("Y","M"), c("M","O"), c("Y","O"))；
NULL则自动生成 group_levels 的全部两两组合}

\item{p_adjust_method}{p值校正方法，默认"BH"}

\item{facet_nrow}{facet的行数，默认2}

\item{output_prefix}{输出文件名前缀，默认"CTprop"}

\item{save_plots}{是否自动保存PDF+PNG，默认TRUE}

\item{fig_width, fig_height}{保存图形的尺寸}

\item{label_outliers}{是否额外生成"标注离群样本ID"的诊断版本图，默认FALSE}

\item{outlier_coef}{IQR倍数阈值，默认1.5}

\item{verbose}{是否打印中间信息，默认TRUE}
}
\value{
list，包含：
  \item{prop_df}{donor-level细胞比例明细表}
  \item{kw_test}{每个celltype的Kruskal-Wallis总检验结果}
  \item{pairwise_test}{每个celltype的两两Wilcoxon检验结果（BH校正）}
  \item{stat_test}{用于画bracket的完整统计表（含坐标）}
  \item{plot}{主图 ggplot对象}
  \item{plot_labeled}{（可选）含离群样本标注的诊断图}
  \item{outlier_df}{离群样本明细表}
}
\description{
计算 donor-level 的细胞类型比例，在指定分组变量（如 AgeGroup_short）
间进行 Kruskal-Wallis 总检验 + 两两 Wilcoxon 检验（BH校正），
并生成 boxplot + jitter 图；可选地将样本点按 dataset/batch 来源
标注 shape+color，用于核查"组间差异是否被单一队列驱动"。
}
\examples{
\dontrun{
res <- ct_proportion_analysis(
  seurat_obj    = immune.combined,
  celltype_col  = "Manuscript_Identity",
  group_col     = "AgeGroup_short",
  group_levels  = c("Y", "M", "O"),
  dataset_col   = "dataset_origin",
  celltype_order = c("EC","Epi","B","DC","Mac","Mono","NK","T","Fib","SMC"),
  celltype_colors = cols,   # 复用你主UMAP的配色向量
  output_prefix = "28_ExtFig1F"
)
res$plot
}
}
