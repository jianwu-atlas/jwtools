% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_te_family_annotation.R
\name{load_te_family_annotation}
\alias{load_te_family_annotation}
\title{加载并校验 TE Family/Class 注释表}
\usage{
load_te_family_annotation(
  path = NULL,
  required_cols = c("scName", "TE_family", "TE_class"),
  verbose = TRUE,
  check_conflict = TRUE
)
}
\arguments{
\item{path}{注释表文件路径(CSV或TSV均可, 自动根据后缀识别分隔符)。
默认为 NULL, 此时使用项目统一维护的清洗后版本(见函数内部代码)。}

\item{required_cols}{必须存在的列名, 默认为 c("scName", "TE_family", "TE_class")。}

\item{verbose}{逻辑值, 是否打印诊断信息。默认为 TRUE。}

\item{check_conflict}{逻辑值, 是否检查scName对应多个不同family/class的数据冲突情况。
默认为 TRUE。}
}
\value{
一个 tibble, 包含三列: scName, TE_family, TE_class。
  附加属性 attr(x, "class_distribution") 和 attr(x, "family_distribution")
  保存了分布统计, 便于设定富集分析阈值参考。
}
\description{
读取转座元件(TE)家族与类别的映射注释表(scName -> TE_family -> TE_class),
并对数据质量进行全面诊断(重复检测、冲突检测、NA值检查、分布概览)。
该函数适用于 LungAgingERV / LungERVIPF / HeartERVmap 等项目中
TE/ERV Family富集分析(compareCluster/超几何检验)前的注释表准备步骤。
}
\examples{
\dontrun{
te_family_map <- load_te_family_annotation()
head(te_family_map)
attr(te_family_map, "class_distribution")

deg_df_annotated <- all_TE_degs |>
  dplyr::left_join(te_family_map, by = c("gene" = "scName"))
}

}
