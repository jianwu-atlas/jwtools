# jwtools 0.2.2

* 版本号例行更新。

# jwtools 0.2.1

* 移除冗余函数 `load_te_family_annotation()`。
* 修复 `ct_proportion_analysis()` 中因 dplyr NSE 导致的 R CMD check NOTE（`n` 变量未定义绑定问题）。
* 清理 `DESCRIPTION` 中未使用的 Imports 声明。

# jwtools 0.1.0

* 初始版本发布。
